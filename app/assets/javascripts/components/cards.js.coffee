App.CardCharacterComponent = Ember.Component.extend(
  classNames: ['card-item']
)

App.CardEventComponent = Ember.Component.extend(
  classNames: ['card-item']
)

App.CardLocationComponent = Ember.Component.extend(
  classNames: ['card-item']
)
