{{#if timeline.ordered_events.length }}
  <h4>Events</h4>
  {{ timeline.started_at }}
  <ul>
    {{#each timeline.ordered_events}}
      <li>{{started_at}}-{{ended_at}} {{#link-to "event" this}}{{name}}{{/link-to}}</li>
    {{/each}}
  </ul>
  {{ timeline.ended_at }}
{{/if }}
