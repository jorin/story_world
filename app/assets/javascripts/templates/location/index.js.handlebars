<br/>
<button {{action 'edit'}} class="btn btn-default">Edit</button>
<button {{action 'delete' this}} {{bind-attr disabled=has_events}} class="btn btn-default">Delete</button>
{{#if has_events}}A location associated with events can't be deleted{{/if}}

<h4>Events</h4>
{{events-timeline timeline=this}}
{{events-list timeline=this}}
